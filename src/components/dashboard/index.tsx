import React, { useEffect, useState } from 'react';
import { ApiClient } from 'adminjs/types/src';

const api = new ApiClient();

const Dashboard = () => {
    
    const [data, setData] = useState()

    useEffect(() => {
        
    }, []);

    return (
        <div>

        </div>
    )
};

export default Dashboard;